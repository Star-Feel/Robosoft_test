from setuptools import find_packages, setup

setup(
    name='ssim',
    version='0.1.0',
    packages=find_packages(include=['ssim']),
    install_requires=[],
    description='ssim',
    author='Ziyu Wei',
    author_email='weiziyu99@gmail.com',
)
